spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:file:test?mode=memory&cache=shared
    hikari:
      maximum-pool-size: 1
      minimum-idle: 1
  sql:
    init:
      mode: always

app:
  storage:
    dir: /tmp/buchoipark-test-uploads
